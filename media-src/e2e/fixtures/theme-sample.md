# Theme fidelity sample (H1 with bottom border)

A paragraph with **bold**, *italic*, ~~strikethrough~~, `inline code`, and a
[link to somewhere](https://example.com). Some more text so a line actually wraps and we
can see the body line-height and measure against GitHub's own rendering of the same source.

## Second-level heading (also bordered)

### Third-level heading (no border)

> A blockquote — text colour, left border, and padding are all theme-driven.
>
> > A nested blockquote to check border + indentation stacking.

- Unordered list item one
- Item two with `inline code` inside
  - Nested item
- Item three

1. Ordered item one
2. Ordered item two
3. Ordered item three

- [x] A completed task-list item
- [ ] An open task-list item

| Column A | Column B | Column C |
| --- | --- | --- |
| row 1 a | row 1 b | row 1 c |
| row 2 a | row 2 b | row 2 c |
| row 3 a | row 3 b | row 3 c |

A fenced code block (palette: panel background + base text; syntax colour is hljs-side):

```js
function greet(name) {
  const msg = `hello, ${name}`
  return msg.toUpperCase()
}
```

Text before a horizontal rule.

---

Text after the horizontal rule, to check spacing around `<hr>`.
